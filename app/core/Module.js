/**
 * Base Class for all Views
 */
var Module = Giraffe.Module.extend({

	/**
	 * This is how we do it round here.
	 */
	templateStrategy: 'jst',

	/**
	 * Default template method
	 */
	template: function () {
	}

});

module.exports = Module;
