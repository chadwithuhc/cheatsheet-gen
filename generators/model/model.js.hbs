var Model = require('core/Model');

/**
 * {{#camelize}}{{name}}{{/camelize}} Model
 * @extends Model
 */
var {{#camelize}}{{name}}{{/camelize}}Model = Model.extend({

});

module.exports = {{#camelize}}{{name}}{{/camelize}}Model;
